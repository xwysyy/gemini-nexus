{
  "name": "gemini-nexus",
  "private": true,
  "version": "4.2.8",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:extension": "npm run build && node scripts/prepare-dist.mjs",
    "package": "npm run build:extension && node scripts/zip-dist.mjs",
    "preview": "vite preview"
  },
  "dependencies": {

  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
